import { createFileRoute, Link } from "@tanstack/react-router";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { supabase } from "@/integrations/supabase/client";
export const Route=createFileRoute("/reset-password")({head:()=>({meta:[{title:"Nova senha — BIGTEAM"},{name:"description",content:"Defina uma nova senha para sua conta BIGTEAM."},{property:"og:title",content:"Nova senha — BIGTEAM"},{property:"og:description",content:"Defina uma nova senha para sua conta BIGTEAM."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Reset});
function Reset(){const [password,setPassword]=useState("");const [message,setMessage]=useState("");const recovery=typeof window!=="undefined"&&(window.location.hash.includes("type=recovery")||window.location.search.includes("type=recovery"));async function submit(e:React.FormEvent){e.preventDefault();const {error}=await supabase.auth.updateUser({password});setMessage(error?error.message:"Senha atualizada. Você já pode entrar.")}return <main className="auth-page"><div className="auth-panel"><p className="text-xs font-bold uppercase tracking-[.2em] text-primary">BIGTEAM</p><h1 className="mt-3 text-4xl font-black uppercase">Nova senha</h1>{!recovery?<p className="mt-4 text-sm text-muted-foreground">Abra esta página pelo link enviado ao seu e-mail.</p>:<form onSubmit={submit} className="mt-7 space-y-4"><label className="field">Nova senha<Input type="password" minLength={6} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{message&&<p className="text-sm">{message}</p>}<Button className="w-full">Atualizar senha</Button></form>}<Button asChild variant="link" className="mt-4 px-0"><Link to="/auth">Voltar ao login</Link></Button></div></main>}
